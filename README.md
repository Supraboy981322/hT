# hT (HTTP thingy)

A tiny HTTP server intended to be used as a quick command to serve files.

---

## Installation

Install with Go install
```sh
go install github.com/Supraboy981322/hT@latest
```
Please make sure that you have your GOBIN in your `$PATH` (typically `~/go/bin`)
